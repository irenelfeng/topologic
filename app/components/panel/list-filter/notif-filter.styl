#notif-filter-container
  width 100%
  flexbox(flex)
  flex-direction(row)
  flex-wrap(nowrap)

  .notif-filter-option
    width 100%
    height 20px

    &.selected
      background-color black

#notif-filter-alarm-img
  height 100%
  width auto
